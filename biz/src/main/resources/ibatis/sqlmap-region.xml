<?xml version="1.0" encoding="GB2312"?>
<!DOCTYPE sqlMap
	PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="region">
	
	<typeAlias alias="region" type="com.hsh24.dms.api.region.bo.Region" />
	
	<select id="getRegion" parameterClass="region" resultClass="region">
	<![CDATA[
		SELECT t.region_id AS regionId,
			   t.parent_region_id AS parentRegionId,
			   t.region_name AS regionName
		  FROM mdm.mdm_tb_region t
		 WHERE t.state = 'U'
		   AND t.region_id = #regionId#
	]]>
	</select>
	
</sqlMap>